# ---------------------------------------------------------------------------
# INDIA UNION BUDGET ANALYSIS SYSTEM  --  Python dependencies
# ---------------------------------------------------------------------------
# Install everything with:
#     pip install -r requirements.txt
#
# NOTE on PDF extraction:
#   camelot-py needs the Ghostscript system binary for its "lattice" mode.
#     - Windows : install from https://ghostscript.com/releases/gsdnld.html
#     - macOS   : brew install ghostscript
#     - Linux   : sudo apt-get install ghostscript
#   If Ghostscript / Camelot is not available, the app automatically falls
#   back to pdfplumber (pure Python, no system dependencies), so it still runs.
# ---------------------------------------------------------------------------

streamlit>=1.36
pandas>=2.2
numpy>=1.26
plotly>=5.22

# --- PDF table extraction (primary + fallback) ---
camelot-py[base]>=0.11
pdfplumber>=0.11

# --- Excel export ---
openpyxl>=3.1
XlsxWriter>=3.2
